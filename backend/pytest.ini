[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    integration: loads real model weights; skips when a component's assets are absent
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
